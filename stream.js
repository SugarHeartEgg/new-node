'use strict'

/**
 *
 * "流" ?
 *        是一种很抽象的数据结构 , 从某一开始并持续不断的到某一结束的过程称之为流 .
 *
 *        借用 廖雪峰老师的比喻:
 *                             键盘输入字符到应用程序     标准输入流
 *                             应用程序把字符给到显示器   标准输出流
 * 
 *        流的特点是有序的 , 而且必须依次的读取 , 或者依次的写入 , 不能像数组那样 随机定位 . 
 *
 */